# 2026-03-02 — MyCoach Sprint

## Utilisateur
- Gael (gaelbeard) — développeur/fondateur MyCoach
- App de coaching sportif Flutter + FastAPI
- Repo: github.com/gaelgael5/mycoach
- Serveur dev: 192.168.10.63 (Docker, Proxmox)
- Firebase: mycoach-c4690

## Travail effectué

### CI/CD
- GitHub Actions: build Flutter → APK
- GitHub Releases avec APK téléchargeable (public)
- Firebase App Distribution (auto-distribute au groupe "testers")
- versionCode auto-incrémenté par run number
- Multi-env config (dart-define): dev/staging/prod

### Corrections
- Fix: cleartext traffic (Android bloque HTTP par défaut)
- Fix: API routes alignées avec backend (/coaches/*, pas /api/v1)
- Fix: Port 8200 (nginx) pas 8000 (portainer)
- Fix: confirm_password manquant dans register
- Fix: api_key au lieu de access_token dans réponse backend
- Fix: phone field retiré de register (pas dans le schema backend)
- Fix: password min 8 chars (backend requirement)
- Fix: password trim pour copier-coller

### Features ajoutées
- Eye icon toggle visibilité mot de passe
- Sélecteur pays international (IntlPhoneField)
- videoUrl sur exercices (modèle + UI + play button)
- Splash screen animé avec auto-auth check
- Login redesigné (welcome text, Google btn placeholder, forgot pwd)
- Dashboard avec stats dynamiques + clients récents
- Clients list avec barre de recherche
- Client detail avec SliverAppBar gradient
- User model enrichi (avatar, status, locale, etc.)
- AppColors complet (design system tokens)

### Backend (déjà déployé par un autre agent)
- FastAPI sur Docker (mycoach-api)
- PostgreSQL 16
- Nginx reverse proxy port 8200
- Watchtower pour auto-updates

### Infos sensibles
- ⚠️ Mot de passe SSH root partagé dans Discord — DOIT ÊTRE CHANGÉ
- Firebase service account key configurée en GitHub secret

### Features ajoutées (session 2, ~10h)
- Inscription refaite : "Je suis un professionnel" + secteur d'activité (Coach, Yoga, Pilates, etc.)
- Invitation client via enrollment tokens (FAB "Inviter" → share link)
- Mot de passe oublié (écran complet + endpoint /auth/forgot-password)
- CI concurrency (cancel-in-progress pour éviter les builds en file)
- Dashboard redesigné avec stats dynamiques (clients, programmes, messages)
- Client detail avec SliverAppBar gradient + stats + actions bottom bar
- Clients list avec barre de recherche + badge coloré X/15
- Routes API alignées avec backend (/coaches/*, /performances/*)
- Splash screen animé avec auto-auth check
- Login redesigné per maquettes UX

### Mission déléguée
- UX-researcher → design showcase app (screenshots Play Store, bannière marketing, landing page concept) → canal #ux-research

### Architecture backend (découvert en session)
- Backend n'a PAS d'ajout direct de client → système d'enrollment tokens
- Docker image: blackbeardteam/mycoach-api:latest (Docker Hub)
- Watchtower auto-pull les nouvelles images
- Pas de CI GitHub Actions pour le backend encore → Gael intéressé pour en mettre un
- Credentials Docker Hub nécessaires pour CI backend

### Discussion en cours
- Gael demande si GitHub Actions peut livrer le backend (image Docker) → proposé Option A (GH Actions → Docker Hub → Watchtower)

## Backlog restant
- CI backend (GitHub Actions → Docker Hub)
- Google Sign-In (nécessite config OAuth Firebase)
- Chat/messaging (backend n'a pas d'endpoint conversations)
- Agent QA testeur (Gael va le configurer lui-même)
- Landing page marketing
- Vidéos démo exercices (intégration YouTube)
